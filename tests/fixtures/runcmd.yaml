#cloud-config
runcmd:
  - echo "Hello from cloud-init"
  - [mkdir, -p, /opt/myapp]
  - ["bash", "-c", "echo 'test' > /tmp/output"]
  - systemctl enable nginx
runcmd_config:
  shell: /bin/bash
  error_handling: continue
