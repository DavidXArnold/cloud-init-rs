#cloud-config
users:
  - default
  - name: testuser
    gecos: Test User
    groups: sudo, docker
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    ssh_authorized_keys:
      - ssh-rsa AAAAB3NzaC1... test@example.com
groups:
  - docker
  - admin: [testuser]
